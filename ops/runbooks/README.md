# runbooks

Short operational docs for when things break or need doing: deploy/rollback,
rotating Stripe keys, replaying failed webhooks, handling a stuck recovery,
and on-call notes. One markdown file per scenario.
