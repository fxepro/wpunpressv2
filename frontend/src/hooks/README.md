# hooks

React hooks:

- `useRecover` — worker lifecycle (free preview → paid export), result state.
- `useEntitlement` — download-unlock state, talks to the backend.
- `useDropzone` — drag/drop + file-input wiring.
