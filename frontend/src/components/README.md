# components

Reusable presentational components extracted from `app/page.tsx` as it grows:
`Hero`, `Dropzone`, `Results`, `Tabs`, `FormatsTable`, `Pricing`, `Faq`, `CtaBand`.
Keep them stateless where possible; state lives in `app/` or `hooks/`.
